[project]
name = "cli-kpis"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lxml>=6.1.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.3",
]
